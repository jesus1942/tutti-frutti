services:
  - type: web
    name: tutti-frutti-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn simple_app:app --host 0.0.0.0 --port $PORT
